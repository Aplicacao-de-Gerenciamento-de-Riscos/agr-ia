# ai_model.py
import joblib
import numpy as np

# Carregar o modelo de IA treinado
# model = joblib.load('my_trained_model.pkl')


# def predict_delay(issues):
#     # Suponha que 'issues' é uma lista de dicionários com as features para o modelo
#     X = np.array([[
#         issue['time_estimate'],
#         issue['timespent'],
#         issue['priority_encoded'],  # Transformar 'priority' em uma feature numérica
#         issue['status_encoded'],  # Transformar 'status' em uma feature numérica
#     ] for issue in issues])
#
#     # Fazer a previsão de atraso
#     return model.predict(X)

